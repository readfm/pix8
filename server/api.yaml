http:
  port: 4251
  main: "localhost"

localhost:
  path: '../'

ws:
  port: 4252

https:
  port: 443

password: "abc"

production: false
modules:
  - functions
  - query
  - site
  - socket
  - fs

loadSet:
  - lastSync

fs:
  collection: 'files'

path:
  modules: './modules/'
  files: '../www/files/'
  tmp: '../www/tmp/'
  ffmpeg: "./bin/ffmpeg.exe"
  ffprobe: "C:/lib/ffmpeg/bin/ffprobe.exe"
  youtube: './files/tube/'
  certificates: 'certificates/'

devMode: true
currency: EUR
lang: en

key: mk

wait: false

http:
  port: 4251
  main: "localhost"

localhost:
  path: '../'

ws:
  port: 4252

https:
  port: 443

password: "abc"

production: false
modules:
  - functions
  - query
  - site
  - socket
  - fs

loadSet:
  - lastSync

fs:
  collection: 'files'
  allowed:
    - 'C:/8'
    - 'C:/88'
  folder:
    item: 'pix8.json'

path:
  modules: './modules/'
  files: './upl/'
  tmp: './tmp/'
  ffmpeg: "./bin/ffmpeg.exe"
  ffprobe: "C:/lib/ffmpeg/bin/ffprobe.exe"
  youtube: './tube/'
  certificates: 'certificates/'

devMode: true
currency: EUR
lang: en

key: mk

wait: false
